import { ArrowRight, BedDouble, Check, MapPin, ShieldCheck, Users } from "lucide-react";
import { Link, useLocation } from "react-router-dom";

const content = { "/universities": { eyebrow: "For university communities", title: "Make finding a safe place to live part of the student experience.", text: "Give students a clearer path to trusted accommodation while building visibility for approved hostel partners.", cards: [[ShieldCheck, "Approved accommodation", "Help students discover verified partners close to campus."], [Users, "Better student support", "Reduce search friction and give every student a reliable starting point."], [MapPin, "Local visibility", "Keep accommodation information easy to compare and simple to access."]] }, "/how-it-works": { eyebrow: "How arc() works", title: "From search to move-in, without the guesswork.", text: "arc() brings discovery, verification, booking and support into one calm experience for students and property teams.", cards: [["01", "Tell us what you need", "Choose your university, budget, preferred location and room type."], ["02", "Compare your options", "Explore verified hostels, rooms, amenities, prices and availability."], ["03", "Reserve your space", "Apply or reserve securely, then manage your stay in one place."]] } } as const;
export default function PublicPage() { const page = content[useLocation().pathname as keyof typeof content] ?? content["/how-it-works"]; return <div className="min-h-screen bg-[#faf9f7] text-[#171717]"><header className="border-b border-white/10 bg-black text-white"><div className="mx-auto flex h-[74px] max-w-[1240px] items-center justify-between px-5 lg:px-8"><Link to="/" className="font-display text-[25px] font-extrabold tracking-[-.08em]">arc<span className="text-[#f5b544]">()</span></Link><nav className="hidden items-center gap-7 text-[13px] font-medium text-white/70 md:flex"><Link to="/find-a-hostel">Find hostels</Link><Link to="/universities">Universities</Link><Link to="/how-it-works">How it works</Link></nav><Link to="/login" className="rounded-full bg-white px-4 py-2 text-xs font-bold text-black">Log in</Link></div></header><main className="mx-auto max-w-[1100px] px-5 py-20 lg:px-8 lg:py-28"><p className="eyebrow text-[#a36500]">{page.eyebrow}</p><h1 className="section-title mt-4 max-w-[800px]">{page.title}</h1><p className="mt-6 max-w-[560px] text-base leading-7 text-black/55">{page.text}</p><Link to="/find-a-hostel" className="mt-8 inline-flex rounded-full bg-black px-6 py-3.5 text-sm font-bold text-white">Explore hostels <ArrowRight className="ml-2 h-4 w-4" /></Link><div className="mt-20 grid gap-4 md:grid-cols-3">{page.cards.map(card => { const [icon, title, text] = card as readonly [typeof ShieldCheck | string, string, string]; const Icon = typeof icon === "string" ? null : icon; return <div className="rounded-2xl border border-black/10 bg-white p-6" key={title}>{Icon ? <Icon className="h-6 w-6 text-[#a36500]" /> : <span className="font-display text-3xl font-extrabold text-[#f5b544]">{typeof icon === "string" ? icon : ""}</span>}<h2 className="mt-12 font-display text-xl font-extrabold">{title}</h2><p className="mt-2 text-sm leading-6 text-black/55">{text}</p></div>; })}</div><div className="mt-20 rounded-2xl bg-[#f5b544] p-8 md:p-12"><div className="flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="eyebrow">Ready when you are</p><h2 className="mt-3 font-display text-3xl font-extrabold">Your next room is closer than you think.</h2></div><Link to="/find-a-hostel" className="w-fit rounded-full bg-black px-5 py-3 text-sm font-bold text-white">Find a hostel <ArrowRight className="ml-2 inline h-4 w-4" /></Link></div></div></main></div>; }
