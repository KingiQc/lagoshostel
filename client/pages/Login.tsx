import { ArrowLeft, ArrowRight, Check, Eye, EyeOff, LockKeyhole, Mail } from "lucide-react";
import { Link, useNavigate } from "react-router-dom";
import { useState } from "react";

export default function Login() {
  const [showPassword, setShowPassword] = useState(false);
  const [role, setRole] = useState<"student" | "owner">("student");
  const navigate = useNavigate();
  return <div className="min-h-screen bg-[#151515] text-white"><header className="mx-auto flex h-[82px] max-w-[1240px] items-center justify-between px-5 lg:px-8"><Link to="/" className="font-display text-[25px] font-extrabold tracking-[-0.08em]">arc<span className="text-[#f5b544]">()</span></Link><Link to="/" className="text-xs font-bold text-white/60 transition hover:text-white"><ArrowLeft className="mr-2 inline h-4 w-4" /> Back to home</Link></header><main className="mx-auto grid max-w-[1240px] gap-12 px-5 py-10 lg:grid-cols-[.9fr_1.1fr] lg:items-center lg:px-8 lg:py-20"><section className="hidden lg:block"><p className="eyebrow text-[#f5b544]">Welcome back</p><h1 className="mt-4 max-w-[540px] font-display text-6xl font-extrabold leading-[.92] tracking-[-.07em]">The easier way to manage <span className="text-[#f5b544]">student living.</span></h1><div className="mt-10 space-y-4 text-sm text-white/60">{["Find and reserve your next room", "Keep every booking and payment in one place", "Get support whenever you need it"].map(item => <p key={item}><Check className="mr-3 inline h-4 w-4 text-[#f5b544]" />{item}</p>)}</div></section><section className="mx-auto w-full max-w-[480px] rounded-[28px] bg-white p-7 text-[#171717] shadow-2xl sm:p-10"><div className="mb-8"><p className="eyebrow">Your account</p><h2 className="mt-2 font-display text-3xl font-extrabold tracking-tight">Log in to arc()</h2><p className="mt-2 text-sm text-black/50">Pick up right where you left off.</p></div><div className="mb-6 grid grid-cols-2 gap-2 rounded-full bg-[#f2f1ee] p-1"><button onClick={() => setRole("student")} className={`rounded-full py-2.5 text-xs font-bold transition ${role === "student" ? "bg-black text-white" : "text-black/50"}`}>I’m a student</button><button onClick={() => setRole("owner")} className={`rounded-full py-2.5 text-xs font-bold transition ${role === "owner" ? "bg-black text-white" : "text-black/50"}`}>I’m an owner</button></div><form onSubmit={e => {e.preventDefault(); navigate(role === "student" ? "/dashboard" : "/owner/dashboard")}} className="space-y-4"><label className="block text-xs font-bold">Email address<div className="mt-2 flex items-center gap-2 rounded-xl border border-black/15 px-3"><Mail className="h-4 w-4 text-black/35" /><input type="email" placeholder="you@example.com" required className="w-full bg-transparent py-3 text-sm outline-none" /></div></label><label className="block text-xs font-bold">Password<div className="mt-2 flex items-center gap-2 rounded-xl border border-black/15 px-3"><LockKeyhole className="h-4 w-4 text-black/35" /><input type={showPassword ? "text" : "password"} placeholder="Enter your password" required className="w-full bg-transparent py-3 text-sm outline-none" /><button type="button" onClick={() => setShowPassword(!showPassword)} aria-label="Toggle password visibility">{showPassword ? <EyeOff className="h-4 w-4 text-black/40" /> : <Eye className="h-4 w-4 text-black/40" />}</button></div></label><div className="flex justify-end"><button type="button" className="text-xs font-bold text-[#a36500]">Forgot password?</button></div><button className="w-full rounded-full bg-black py-3.5 text-sm font-bold text-white transition hover:bg-[#f5b544] hover:text-black">Log in <ArrowRight className="ml-2 inline h-4 w-4" /></button></form><div className="my-6 flex items-center gap-3 text-xs text-black/30"><span className="h-px flex-1 bg-black/10" />or<span className="h-px flex-1 bg-black/10" /></div><button className="w-full rounded-full border border-black/15 py-3 text-sm font-bold">Continue with Google</button><p className="mt-6 text-center text-xs text-black/50">Don’t have an account? <button className="font-bold text-[#a36500]">Create one</button></p></section></main></div>;
}
