import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { ArrowLeft, ArrowRight, CheckCircle2, Download, MessageCircle, Phone, XCircle } from "lucide-react";

type Booking = { id: string; propertyName: string; propertyLocation: string; roomName: string; price: string; status: string; createdAt: string };
function readBooking(): Booking | null { try { const saved = window.localStorage.getItem("arc.student.booking"); return saved ? (JSON.parse(saved) as Booking) : null; } catch { return null; } }
function Empty() { return <div className="min-h-screen bg-[#f7f6f3] text-[#171717]"><header className="border-b border-white/10 bg-black text-white"><div className="mx-auto flex h-[74px] max-w-[1120px] items-center justify-between px-5 lg:px-8"><Link to="/" className="font-display text-[25px] font-extrabold tracking-[-.08em]">arc<span className="text-[#f5b544]">()</span></Link><Link to="/student" className="text-xs font-bold text-white/70">Student dashboard <ArrowRight className="ml-1 inline h-3 w-3" /></Link></div></header><main className="mx-auto flex min-h-[calc(100vh-74px)] max-w-[620px] items-center justify-center px-5 py-16 text-center"><div><CheckCircle2 className="mx-auto h-10 w-10 text-black/25" /><h1 className="mt-5 font-display text-3xl font-extrabold">No booking to show</h1><p className="mt-3 text-sm leading-6 text-black/50">Confirmed bookings will appear here after you complete a verified property checkout.</p><Link to="/find-a-hostel" className="mt-6 inline-flex rounded-full bg-black px-5 py-3 text-xs font-bold text-white">Find a hostel <ArrowRight className="ml-2 h-4 w-4" /></Link></div></main></div>; }
export default function BookingDetails() { const { id } = useParams(); const [booking, setBooking] = useState<Booking | null>(null); const [cancelOpen, setCancelOpen] = useState(false); const [cancelled, setCancelled] = useState(false); const [notice, setNotice] = useState(""); useEffect(() => { const stored = readBooking(); setBooking(stored && (!id || id === "current" || stored.id === id) ? stored : null); }, [id]); if (!booking) return <Empty />; const downloadReceipt = () => { const receipt = ["arc() BOOKING RECEIPT", "", `Booking ID: ${booking.id}`, `Property: ${booking.propertyName}`, `Room: ${booking.roomName}`, `Location: ${booking.propertyLocation}`, `Amount: ${booking.price}`, `Status: ${cancelled ? "Cancellation requested" : "Confirmed"}`].join("\n"); const url = URL.createObjectURL(new Blob([receipt], { type: "text/plain" })); const link = document.createElement("a"); link.href = url; link.download = `arc-booking-${booking.id}.txt`; link.click(); URL.revokeObjectURL(url); setNotice("Receipt downloaded"); window.setTimeout(() => setNotice(""), 2500); }; const cancel = () => { const next = { ...booking, status: "cancellation_requested" }; window.localStorage.setItem("arc.student.booking", JSON.stringify(next)); setBooking(next); setCancelled(true); setCancelOpen(false); setNotice("Cancellation request submitted"); }; return <div className="min-h-screen bg-[#f7f6f3] text-[#171717]"><header className="border-b border-white/10 bg-black text-white"><div className="mx-auto flex h-[74px] max-w-[1120px] items-center justify-between px-5 lg:px-8"><Link to="/" className="font-display text-[25px] font-extrabold tracking-[-.08em]">arc<span className="text-[#f5b544]">()</span></Link><Link to="/student" className="text-xs font-bold text-white/70">Student dashboard <ArrowRight className="ml-1 inline h-3 w-3" /></Link></div></header><main className="mx-auto max-w-[1000px] px-5 py-10 lg:px-8 lg:py-14"><Link to="/student/bookings" className="mb-8 inline-flex items-center text-xs font-bold text-black/50 hover:text-black"><ArrowLeft className="mr-2 h-4 w-4" /> Back to bookings</Link><div className="flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="eyebrow">{cancelled ? "Cancellation requested" : "Booking confirmed"}</p><h1 className="mt-2 font-display text-4xl font-extrabold tracking-[-.06em]">{booking.propertyName}</h1><p className="mt-2 text-sm text-black/50">{booking.propertyLocation} · {booking.roomName}</p></div><span className={`w-fit rounded-full px-4 py-2 text-xs font-bold ${cancelled ? "bg-[#ffe3e3] text-[#a52a2a]" : "bg-[#dff3e6] text-[#16733b]"}`}><CheckCircle2 className="mr-2 inline h-4 w-4" />{cancelled ? "Cancellation requested" : "Confirmed"}</span></div><section className="mt-8 rounded-2xl bg-black p-6 text-white sm:p-8"><div className="flex items-center justify-between"><div><p className="text-xs text-white/45">Booking ID</p><p className="mt-1 font-display text-2xl font-extrabold">{booking.id}</p></div><CheckCircle2 className="h-8 w-8 text-[#74d69a]" /></div><div className="mt-8 grid gap-5 border-t border-white/10 pt-6 sm:grid-cols-3"><div><p className="text-xs text-white/45">Property</p><p className="mt-1 text-sm font-bold">{booking.propertyName}</p></div><div><p className="text-xs text-white/45">Room</p><p className="mt-1 text-sm font-bold">{booking.roomName}</p></div><div><p className="text-xs text-white/45">Amount</p><p className="mt-1 text-sm font-bold">{booking.price}</p></div></div></section>{notice && <div className="mt-4 rounded-xl bg-[#dff3e6] px-4 py-3 text-xs font-bold text-[#16733b]" role="status">{notice}</div>}<div className="mt-6 grid gap-5 lg:grid-cols-[1fr_300px]"><section className="rounded-2xl border border-black/10 bg-white p-6"><h2 className="font-display text-xl font-extrabold">Manage booking</h2><div className="mt-5 grid gap-3 sm:grid-cols-2"><button onClick={downloadReceipt} className="flex items-center justify-center gap-2 rounded-full border border-black/15 py-3 text-xs font-bold"><Download className="h-4 w-4" /> Download receipt</button><button onClick={() => setNotice("Messaging will be available when the hostel connection is live.")} className="flex items-center justify-center gap-2 rounded-full border border-black/15 py-3 text-xs font-bold"><MessageCircle className="h-4 w-4" /> Message hostel</button><button onClick={() => setNotice("Support calling will be available when support contact data is connected.")} className="flex items-center justify-center gap-2 rounded-full border border-black/15 py-3 text-xs font-bold"><Phone className="h-4 w-4" /> Contact support</button>{!cancelled && <button onClick={() => setCancelOpen(true)} className="flex items-center justify-center gap-2 rounded-full border border-[#a52a2a]/20 py-3 text-xs font-bold text-[#a52a2a]"><XCircle className="h-4 w-4" /> Request cancellation</button>}</div></section><aside className="rounded-2xl border border-black/10 bg-white p-6"><p className="eyebrow">Booking status</p><div className="mt-5 space-y-4 text-xs"><p className="flex items-center gap-3"><span className="flex h-6 w-6 items-center justify-center rounded-full bg-[#dff3e6] text-[#16733b]"><CheckCircle2 className="h-4 w-4" /></span>Request received</p><p className="flex items-center gap-3 text-black/45"><span className="h-6 w-6 rounded-full border border-black/15" />Move-in details pending</p></div></aside></div>{cancelOpen && <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/70 p-4"><div className="w-full max-w-[420px] rounded-2xl bg-white p-7"><h2 className="font-display text-2xl font-extrabold">Request cancellation?</h2><p className="mt-2 text-sm leading-6 text-black/55">The hostel will review your request. This preview will mark the booking as cancellation requested.</p><div className="mt-6 flex gap-3"><button onClick={() => setCancelOpen(false)} className="flex-1 rounded-full border border-black/15 py-3 text-xs font-bold">Keep booking</button><button onClick={cancel} className="flex-1 rounded-full bg-black py-3 text-xs font-bold text-white">Confirm request</button></div></div></div>}</main></div>; }
